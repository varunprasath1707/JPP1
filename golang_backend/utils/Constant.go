package utils

// APP_MODE config.
const (
	APP_MODE_DEV   = "dev"
	APP_MODE_QA    = "qa"
	APP_MODE_STAGE = "stage"
	APP_MODE_PROD  = "prod"
)

// App Configuration Keys.
const (
	APP_MODE = "app.mode"
	APP_PORT = "app.port"
)

// AWS Configuration Keys.
const (
	AWS_REGION                = "aws.region"
	AWS_ACCESSKEY             = "aws.access_key"
	AWS_SECRETKEY             = "aws.secret_key"
	AWS_CLOUDWATCH_LOG_GROUP  = "aws.cloudwatch.log.group"
	AWS_CLOUDWATCH_LOG_STREAM = "aws.cloudwatch.log.stream"
)
